def csvToList(strIn):
    counter = 0
    if "\n" in strIn:
        while (strIn[counter] != "\n"):
            counter += 1








    else:
        raise("String inputted is not in csv format")
        return None
